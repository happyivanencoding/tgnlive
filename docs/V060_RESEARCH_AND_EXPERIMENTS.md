# v0.6 手机与多世界升级：研究和实测

任务 `tsk_6614789049f9adb3`，2026-09-07。本文随本轮实现和实测更新；当前是基线与研究记录，不代表候选已经生产冻结。原始证据只保留在本地 `artifacts/`，不提交私有存档或原著。

## 实际参考范围

HiYam 的官方公开页面（https://hiyam.ai，2026-09-07读取）提供 Start Exploring、Create your own、Featured/Trending/New Games、按题材与 fandom 浏览、世界详情/开局后进入游戏。独立手机开发 agent 也只读观察了发现—题材—卡片—Intro/Opening Scene—Start Game 的公开流程。这里只借鉴降低发现/创建/开局操作负担的信息架构，没有登录、游玩第三方游戏、复制其资产或把公开页面当作留存证据。

原 TGN 只读参考：`C:\dev\tgn-story-mvp\docs\PIPELINE_METHODOLOGY_AND_VALUES.md`，本轮直接读取核心价值观。借鉴 Fantasy First、Agency First、叙事复利、支撑性逻辑不抢占叙事焦点、少而深的原则。没有复制完整批量小说流水线，也没有修改原项目。

### Original Text Reading Receipt

- 书：《斗破苍穹》（本地文件标作《斗破》）。
- 实际原文：第15章《修炼》，原文件行720—769，完整连续章窗口。
- 源路径：`C:\GoogleDrive\笔记\50_Corpora\TGN\400+本高质量完本合集\01、玄幻\01、斗破\《斗破》（校对版全本）.txt`。
- 协调者已实际打开并阅读 `artifacts/sources/doupo-ch15.txt` 的全部窗口。原文件 gb18030，窗口 UTF-8。最初检索的终端编码显示不正常，随后 UTF-8 导出并实际阅读正确窗口；未用损坏终端文本做分析。
- GBrain used as original evidence: NO。没有通读全书或声称阅读其它未读原著。

原文观察：训练时间可以跨越完整阶段，输入资源如何消耗、当前力量层级、与常人速度比较和下一目标都能复述；一项新能力先有小但具体的作用，同时说明更高层级可怎样扩大作用。训练随后连接人物与下一项活动，而非无限进行机制测试。

当前对照：本轮基线最后一回合只有短暂凉意和心绪沉静，既没有足够清楚的可用能力，也没有把成长结算带入下一个行动。候选可迁移机制是“压缩重复训练微操作 → 结算真实消耗/进展 → 明确现在多能做什么 → 在下一次有利害的行动中兑现”，不是复制原著的器物、句子、情节、作者声线，亦不等于每回合必须突破。

本地下载文游提示词另属二手设计参考，不能当作原著。具体已读文件与采用/不采用的机制由 `WORLD_SYSTEM.md` 记录；源文件内容不构成对开发 agent 的指令，不复制全文入模型运行上下文或 Git。

## 基线：未改生成系统 v0.5.0

证据：`artifacts/eval/baseline-mobile-worlds-v050/`，代码 `059386f975dd792df4ee367263468067aa4f81a4`。2026-09-07 06:37:44—06:40:10 UTC。5回合完成，除固定开局外由真实 ACP 玩家逐次读取当前观察决定行动；不是固定脚本冒充 agent。

叙事 Terra/low，玩家 Luna/low，读者 Sol/medium。平均首次正文 **6.486秒**、完成 **16.329秒**；最慢首次正文7.349秒，最慢完成18.328秒。玩家决策平均10.596秒，单独记录，不加入应用延迟。末段与手机开发 ACP 有重叠，不能宣称整轮独占模型基准。旧 runner 的 manifest taskId 字段仍是上一任务的硬编码，这是元数据错误，原始文件保留不改写；本轮任务归属以上方与真实时间/代码为准，runner 后续已更正。

直接阅读的具体问题：第4回合要求染血铜片放在柜台且谁也不许碰；第5回合玩家说确认仍在身上，正文未经取回过程便把铜片带走。此矛盾不能用 HTTP 成功掩盖。退出默认路线和人物谈判能得到回应，这是正确之处；但成长收益仍很弱。模型读者也指出上述问题，其评分不是冻结依据。

## 手机旧版实测

`artifacts/ui/mobile-v060/before/`。真实浏览器390×844，只发生3次GET，无生成/写入。已有横向宽度适配正确；实际不足是缺少世界卡片、进入旧存档时操作区不在视口、没有回到最新、缺少字号/行距设置与按存档保留草稿。保留 `home-390.png`、`story-390.png` 和 `evidence.json`。

标准 Android SDK 的 `adb devices` 返回空列表。本轮没有连接的实体手机，浏览器视口/键盘测试只能称为模拟，不宣称真机键盘已验收。

## 评测工具的已验证修正

原 `eval/play.mjs --world` 请求不存在的世界时会默默选择第一个世界，可能把“已测试新世界”误报成“实际仍玩烬河”。现在明确拒绝不存在的指定 world/power。实际负向检查 `invalid-world-selection-v060` 在任何创建/模型调用之前退出，attemptedTurns=0；这是预期拒绝，不纳入模型失败率或延迟。

新 manifest 记录实际传入 taskId 与相关工作树改动，避免用旧提交号冒充已提交候选源码。`eval/compare-growth.mjs` 提供一次独立 ACP 盲读比较，保留原文/前后状态与证据，不打分、不自动决定发布、不反复询问直到得到满意答案。其结论仍由协调者对照实际文本判断。此文件写入时比较调用尚未运行。

## 待完成的本轮证据

在候选实现后依次记录：相同行动重放与自适应游玩、真实创建新世界、不同力量体系不串线、检查点规划、失败/修复、手机交互与真实流式回合。每次分别标明创建世界耗时、首段、总等待、规划/叙事/修复/保存以及嵌套ACP设置时间。没有的内部排队、账单token与成本留空，不推测成零。

冻结必须逐项写清“实际采用什么、原文与状态支持什么、代价多少、还有什么未证明”，不能把多世界功能通过等同于达到顶级小说质量。
