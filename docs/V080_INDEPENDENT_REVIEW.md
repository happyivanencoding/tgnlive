# v0.8 independent evidence review — incident fixes and long-play findings

Last evidence update: 2026-09-07, including the independent D run ending 12:14:58 UTC and the principal's completed paired planner probes. This report belongs to the second concurrent ChatGPT invocation of the same user request. The original coordinator owns final `src/**` / `public/**` integration, world presets, deployment, handoffs and Git. Following completion of its mobile ACP, this reviewer applied narrowly tested visible-frame timing and persistent Stop-button fixes to current `public/app.js`; all frozen real-play candidates remain untouched. No foreign work, production save or corpus has been overwritten. Larger shared-layout edits were aborted before writing on detecting the coordinator's concurrent `changeKinds` feedback improvement. This is not release acceptance. The agreed lane is recorded in `.runtime/progression-v080/PRINCIPAL_COORDINATION.md`.

## Latest evidence: candidate D (supersedes earlier blocked/replay snapshots)

At 12:04 UTC the principal's `progression-treatment-stars-18-v080d` has **18 committed turns in the same game**, including the eight inherited C turns and ten D continuation turns. The manifest explicitly retains `continuedFrom`, `inheritedCommittedTurns:8`, and both old failed attempts. D changes the ACP metadata false-positive and UI layout, not the growth prompts/reducer/model effort. This is a legitimate completed 18-turn trajectory, **not a fresh clean 18/18 candidate-D experiment**. The older partial-C observations below are historical, not the current completion status.

The independent `progression-mobile-candidate-d-review` completed all **32 unchanged incident checks**, with no browser errors. Preview/final height difference is now **0 px**, dock height difference on submit and commit **0 px**, and reader-up displacement **0 px**. Offscreen text remains unmarked until a real return to it. Stop focus, old paragraph anchors, 12 px minimum, 16 px input, 44 px targets at three mobile widths, RTL/draft/IME, keyboard and reduced-motion checks all pass. This supersedes the 30/32 integration snapshot below. This is replay of real prose with synthetic transport timings, desktop mobile viewports rather than physical handset/WAN testing.

The secondary reviewer's fresh beast-world browser sample `progression-browser-beast-18-v080d` ended at 12:14:58 UTC with **16 committed turns plus one failed attempt at turn17**, satisfying the user's minimum15-turn observation window but not its planned18. The failure was a real120-second **planner session preparation** timeout, not fabricated plot failure or a skipped bad turn. No source/server/production mutation was made by the runner, and its assigned4318 model slot is now free.

### Complete star trajectory: real but limited compounding

Read every committed narrative turn, action, choices, final state and source trace. T5 embeds the tide core rather than postponing it; T6 stabilizes a water-diversion move; T7 actually uses it to cross the flooded steps. T10 uses the negotiated boat passage, and T15 uses it again; the retreat rope created at T12 is used at T15. This is actual execution of acquired access, not merely friendly NPC text. T17 finally opens the independent cashbox and obtains a route document and tangible items. The main talent retains multiple learned local maps.

The result remains far below broad durable identity transformation. Rank is still 0; progress 29 is not itself proof of improved play. Cash rises from20 to30. Of four leverage records, three are limited to tonight's boat/rope/expedition, and the fourth is a newly acquired untested route. There is no actual enduring office, independent revenue stream, permanent travel vehicle, widely recognized identity or demonstrated ability combination. Choices remain dominated by more reconnaissance, negotiation and local safe-route confirmation. The player does exercise real refusal: it limits the lighthouse inspection, withdraws at the agreed warning and pursues its own canal route instead of the boat owner's objective.

State freshness remains a concrete gap. The top-level goal still asks for the first core after its embedding. The cashbox opportunity says `fulfilled/materialized:true` at T16 even though its own result states the contents have not been obtained; the box is actually opened at T17. This is not a new invented ability or arithmetic violation, but it demonstrates that **a ledger label cannot substitute for reading the payoff**. The ledger also does not automatically distinguish a night's useful permission from permanent compounding capital.

### Comparable timing populations: the star treatment did not get faster

Using the standard average-of-middle-two median on **server trace durations for completed requests**, baseline18 has21,182ms median /58,065ms max and the C→D18 trajectory has25,437.5ms median /74,057ms max. Ordinary requests without planner/repair: baseline16 median20,986.5ms, treatment15 median23,698ms. Narrator span medians20,961.5→24,558.5ms; planner medians33,890→35,327ms (only two completed planner calls per trajectory). Context assembly median0ms, combined parse/validation0→1ms, persistence11.5→8ms. Nested ACP-setup spans must not be added twice.

Treatment T9 takes74,057ms (planner33,512 + Narrator40,510); T17 takes67,325ms (37,142 +30,113). A real JSON repair at T3 adds16,896ms. The inherited failed requests of35,282ms and987ms are retained **outside** the completed-request distribution, not erased. Browser first visible-frame and choices medians in the principal's full trajectory summary are6,942.95ms and25,513.2ms; immediate feedback21.8ms. That visibility implementation changed between inherited C and D, so do not use its aggregate as proof that old offscreen-paint bugs never occurred. The fresh reviewer D run measures the same transport independently.

These are small, non-paired evolving player trajectories; they do not isolate a causal latency tax to one field. They do decisively rule out claiming that this star treatment is already faster. Better feedback is an observed UI improvement, not lower model wall time. The baseline API-player clock is not a measured baseline physical-phone paint.

## Fresh beast D: 16 real browser turns and a retained checkpoint failure

This reviewer read all16committed narratives, choices, actions, initial/final inventory and the independent player records. The player sees the public status drawer and last four turns, not the author's hidden plan. The trajectory is **not an A/B against a beast baseline** and not evidence of human retention. Its intent/continuation comments are observations from an AI player, not customer-survey scores.

The payoff sequence contains meaningful changes. T2 delivers the opening talent's shared view; T5 buys actual passage through useful information rather than fetching an arbitrary next clue; T7 executes shared wind-reading to leave the harbor. At T10, talent plus gliding equipment plus the beast's response produce synchronized mid-flight correction and a usable crossing. The companion and navigator both change from guarded interaction to practical cooperation. T13 turns the new technique into a repeatable return traverse, and T14 actually reuses it. At T15, the route information produces treatment, a long rope, two anchor rings and a continuing reciprocal relationship with the veterinarian. This is more than `attitude=friendly`; named people physically help navigate, rig the return line and treat the beast.

The limitations are just as clear. ByT16 the player is still in the first local route episode, at rank0 with24coins and the same two capability IDs (the main talent has gained a richer usable effect). A new map segment is repeatedly proposed after the last one. Three of four leverage records are passage/route access; the fourth is aid conditional on useful route information. Neither a mature contracted-beast build, a second genuinely different skill combination, an independent economic asset nor public social-status change has emerged. Most later recommendations are inspect/extend/retreat from the same route. These observations support accepting the useful-state foundation while **not claiming that the investigation-dominant loop or long-term identity problem is solved**.

The tangible-state audit adds one concrete consistency incident: final `sweet-moss.qty` is2 while its updated description says only1cake remains; the story had already fed one cake to the beast. The UI/next-prompt quantity therefore disagrees with another current-state field. Separately, the worn original gliding rope is still described as supporting one short glide despite several later uses; whether that is an actual one-use item or a load-limit wording issue requires clarification rather than automatically inventing wear or deleting the rope. The repeated `获得长期筹码` label is also too strong for one-journey passage rights; scope must be read and ephemeral rights must not be counted as permanent wealth.

### Measured browser and backend times (completed requests versus failure)

For the16successful requests: browser HTTP headers median11.75ms; first visible waiting feedback26.25ms; first narrative SSE6,614.4ms; independently observed narrative visible frame6,637.35ms; complete received22,391.8ms; usable next-choice visible frame22,446.25ms. Maximum completed wait54,091.6ms and choices54,121.6ms occur on the checkpoint. Player deliberation median5,904.77ms is excluded. Visibility means consecutive intersected frames in desktop Chrome's emulated390pxmobile viewport, **not physical Android/iOS hardware paint, WAN latency or future customer performance**.

Completed backend spans: Narrator median21,635.5ms, nested ACP preparation664.5ms, input validation0ms, request validation2ms, context assembly0ms, parse0ms, reducer0ms and SQLite persistence7ms (max70). The only successful planner call takes35,523ms, including637mssetup; the whole T9 request takes54,058ms. These spans overlap by design; do not sum the ACP child a second time.

T17 then fails in `acp_planner_initialize_auth_session_model_setup` after120,009ms, with a0-character Narrator prompt and0text events. Server whole request120,012ms; browser end120,026.7ms. Trace `trace_91d7257fc2884571b76935a254004278`, game `game_4836759d0def43fe930d3a09bcbd6f55`. `summary.json.allAttempts` retains that failure separately from the16completed statistics. This repeats an intermittent session-preparation class of blocker even though normal setup is subsecond. It does not prove the root cause is rate limiting, model reasoning, localCPU or a particular AgentDock lock.

The browser also retained12 `requestfailed/net::ERR_ABORTED` notifications on the SSE URL, including several after otherwise complete/committed turns. These are raw network-lifecycle observations, not12extra failed game turns;16versions and Canon commits were verified and there were no page-JavaScript exceptions. Their transport cause is not isolated. They remain in the artifact instead of being discarded to make a clean-browser claim.

### Independent reading of the principal's paired planner probes

The fixed-context probes in `artifacts/progression-v080/planner-effort-stars-d-02` completed on the same pre-T9 and pre-T17 star states/actions. They are actual ACP calls, not a replayed timer or a long-play treatment. T9 medium took35.755s and low31.587s; T17 medium31.530s and low29.971s. Lowering effort saves4.168s and1.559s in these two observations. Both still produced usable scene/growth planning, but two contexts do not establish a general quality/speed win, and an effort change does not repair session creation.

The compact-output medium probe in `planner-compact-stars-d-01` removed redundant openings/continuity/milestone lists and kept pressure, two NPC moves and growth want/payoff/afterUse. Output length fell from1,229→626characters atT9 and954→584atT17. Its total times were24.701s and32.694s: **T9 improved11.054s; T17 worsened1.164s** against the corresponding old medium observation. Internal first planner text was later,12.108s/18.968s; it is not user-visible narrative. Full compact plans were read: T9 still emphasizes verification/risk maps, while T17 explicitly allows the already-earned cashbox payoff rather than another qualification. Neither result demonstrates a broadly faster online game, and this two-state probe cannot substitute for a fresh15+turn compact-planner trajectory.

The candidate-E martial launch retained a catalogue/power preflight exclusion, then two actual first-turn preparation timeouts with zero committed turns (`progression-martial-18-v080e2`). These were not failed story reasoning and provide no verdict on the new world's long-term fun. They prevent claiming a clean end-to-end E acceptance from a valid JSON plan alone.

### Release boundary and next narrow decision

D now has evidence across completed18-star and16-beast trajectories and the corrected mobile incidents. The newer **compact planner output contract** visible in current `src/prompts.js` / `GenerationService.validatePlan` was authored after D was frozen. D cannot certify its quality or speed; the principal's paired planner probe and any additional real-play evidence must be reported separately, or that unvalidated change held back. The remaining architectural concern is concrete: an optional periodic planning failure currently blocks the whole player turn even with intact current Canon. A budgeted fallback to current state plus an explicitly advisory prior plan, or no plan, is a plausible small experiment; it is **a recommendation, not an implemented or validated recovery claim**. Canon/reducer checks must not be bypassed, timeout must not simply be lengthened, and abandoned ACP work must remain bounded.

The fresh beast run stopped and released its slot rather than quietly retrying for a passing18. The principal retains release/commit/push ownership; no physical-phone or public-production acceptance is implied by this report.

## New-world long sample: masked C→F18 (completed 12:37:32 UTC)

`progression-masked-18-v080f` now has18committed turns, including the five previously committed C turns and thirteen continued on F. All18actual prose/actions/state changes were independently read. The summary retains18application attempts with0application failures in that exported trajectory, but earlier player-preparation failures live in the predecessor-C artifacts. Do not advertise it as eighteen fresh F turns or proof that the entire historical experiment never failed. This supersedes older statements that no new preset reached15turns.

There is real economic continuity. A produced preservation ink batch sells atT3; the acquired relay map enables a concrete shipment. T14 verifies arrival and sells the parcel for7coupons. T15 then explicitly removes the need to re-prove basic preservation for the next shipment and offers two parcels under one3-coupon small-item fee. That is an actual benefit from an earlier successful delivery, not an unused `friendly` tag. T18 trades the remaining roots for two durable material types, which can wait for cheaper transport without the old short-preservation problem. The player declines the proposed cheap-but-slow vessel and independently shops for more durable goods.

The preset nonetheless remains a **work/low-value-trade loop**, not yet a convincing occult-growth experience: checking ink/signatures, defining liability, earning2coupons, comparing3versus2shipping fees, querying ship timing and material prices. Final rank0/progress9, two initial capability IDs, no acquired supernatural imprint, expanded embodied power or public identity transformation. The two leverage records are a shipment receipt and a conditional buyer trial window. The same bounded economy can be a useful supporting system, but completing18turns does not make this an accepted example of the user's central male-progression fantasy.

Two exact resource-state counterexamples remain. T8 says one bundle of bitter root remains after crafting; T15/T17 refer to two and T18 trades two away. AtT14, the7-coupon sale already increments the authoritative `coins` balance. The final balance is26; T18 prose then calls all26local cash **plus** the same7remaining in the external port account. The ledger still describes a7-coupon claim as usable. This is a representation/semantic-consumption problem, not evidence that the deterministic reducer performed a numeric addition incorrectly. It can double-present spendable wealth to the reader and later prompts. The earlier T3ownership correction and T4incorrect quoted cash remain in the inherited history; they have not been rewritten away.

Summary timing across the mixed18: browser first narrative SSE median6,459.55ms, visible-frame proxy6,475.90ms, complete21,561.50ms, usable choices21,579.65ms, immediate feedback23.20ms; maximum complete68,416.30ms. Player decision6,446.25ms is excluded. C→F changed visibility/transport/planner contracts, and the routes differ from stars and beasts; this is a third-world observed distribution, **not a controlled speedup claim**. F successfully executes a later multi-turn run with20second preparation budgeting/substage tracing present, but no newly observed20second timeout in this run proves that a deliberately stuck upstream setup can no longer leak or finish late. The corresponding cancellation/budget tests and source establish a narrower engineering guarantee.

The new combat preset has no successful15-turn sample because its actual initial calls hit the recorded preparation failures. The final catalogue name is taken from current `docs/PROGRESSION_WORLDS.md` / release handoff (currently `裂陆武途`, world ID `martial-frontier`), not from a stale intermediate name. Neither preset is being claimed to have human retention evidence.

## Shared real baseline: 18 independent ACP turns

Source: `artifacts/eval/progression-baseline-stars-18-v070/` (inherited shared run, not a new run launched by this reviewer). Read the actual `transcript.md`, `final-game.json`, `summary.json` and phase trace. 18/18 accepted, no retries/failures. Player can refuse the suggested task; it did so early. A reusable short-range light-lag capability was earned in turn 4, so the correct verdict is **limited real growth, not no growth**.

By turn 18 the realm remains rank 0 / progress 4, coins remain 20, relations remain the two initial attitudes, and only the turn-4 capability was added to the starting two. The route after that mostly investigates a warning pin, drainage grate, guards, ledger, pressure gauge, inner gate and finally another maintenance hole. Formal core inscription is still a future condition. The only literal `promises` entry is conditional repayment of a borrowed pry tool, not the protagonist's early growth prospect. This supports testing a persistent opportunity / useful-rights account, not forced periodic awards.

There is also a **current-state freshness ambiguity** worth fixing at the existing update boundary rather than with an extra reviewer agent:

- The tide core description still says its carrying test is incomplete. Turn 4 explicitly completed a *short* carrying test and obtained a bounded use; it explicitly did **not** embed the core. The description should distinguish the achieved short use from genuinely unachieved formal embedding. This does **not** authorize automatically declaring full embedding or a breakthrough.
- The wet ledger description still says the waxed record needs examination after turn 13 deciphered that record. Historical facts about finding an unread ledger may remain as history; current item affordances should not keep reinstating that solved task.

These are plausible contributors, not a causally isolated explanation of the entire loop. Neutral ACP player behavior may itself be cautious. The treatment must still be read across multiple worlds and at least 15 turns/sample.

## Latency baseline: do not add nested stages

The shared runner uses a lower-middle convention for even-sample medians. Its reported API first narrative median is 5.791 s, API completion median 21.133 s, maximum 58.080 s. Recompute from raw records using one consistent median convention before a final comparison with the browser runner (which uses the average of two middle observations).

The two checkpoint turns are especially clear: turn 9 spends 32.402 s in Story Brain plus 18.639 s in Narrator; turn 17 spends 35.378 s in Story Brain plus 22.637 s in Narrator. ACP initialization is a child span inside those model spans, not an extra cost to add twice. No repairs occurred. SQLite persistence is 4–29 ms in this sample. `firstReaderVisibleMs` in the old backend is server SSE dispatch, **not browser paint**. Baseline browser latency is unknown, not zero.

## Independent browser replay: actual prose, synthetic timing

Owned executable: `ui-tests/progression-mobile-contract.mjs`. It serves frozen static assets on an ephemeral loopback port and replays already-committed turn 10 of the actual star baseline; it does not call models or write production/evaluation saves. Snapshot path: `.runtime/progression-v080/ui-replay-source.json`. Baseline static assets: `.runtime/progression-v080/ui-baseline-v070/public/`.

Evidence folders under `artifacts/ui/`:

| Record | Scope and actual finding |
| --- | --- |
| `progression-mobile-baseline-v070-01` | Original UI. A real upward wheel of 120 px near the bottom was overridden by subsequent stream chunks, shifting the reader +452 px. Dock starts +60 px and ends -77.391 px. Preview/final section geometry differs -27.078 px. History DOM nodes are replaced on submit and commit. The Stop button loses focus during the 250-ms timer. Minimum 16 px and 30–40 px controls. Existing RTL, IME, draft persistence and synthetic keyboard checks pass. |
| `progression-mobile-treatment-01` | Reviewer-only isolated prototype, **not accepted/shipped**. User-up displacement 0 px; old paragraph identity/position survives; preview/final geometry 0 px; 12 px minimum and 44 px controls. However new-choice height still shrinks the dock 17.391 px at commit. Preserve this failure. |
| `progression-mobile-recorder-smoke-02` | Discarded test-instrumentation attempt: recorder misidentified closed sheets as open because the UI uses `aria-hidden`/`.open`, not the HTML `hidden` property. All visible timing fields were null. Corrected only the test recorder selector; this is **not** a product defect or latency result. |
| `progression-mobile-owner-review-01` | Frozen snapshot of the original UI agent's **in-progress** edits, not its finished release. User-up displacement 0 px and old nodes/anchors preserved; 12 px, 44 px targets and multilingual checks work. Residuals: Stop focus still lost; preview/final -18.578 px; dock starts +70 and ends -70 px; offscreen text is incorrectly recorded by the product as already visible. |

Owner-review visibility counterexample: when the reader stayed near the top, the new turn was 8,991.75 px below the viewport. The independent visible-frame observer correctly remained null, while `getTimings()` claimed a first narrative paint at 21.4 ms. `requestAnimationFrame` after a DOM update alone cannot establish that text is within the unobscured viewport. The final UI needs separate received/DOM/frame-visible measurements; it must not report offscreen text as seen. The isolated reviewer prototype contains a possible implementation, not an instruction to replace the owner's code.

Normal replay verifies the independent recorder itself: HTTP headers 12.1 ms, first narrative SSE 692.8 ms, first narrative visible frame 715.2 ms, complete received 1,493.0 ms, choices visible frame 1,515.1 ms. These numbers are **synthetic replay timings, never model performance**.

All browser evidence so far is desktop Chrome at 360/390/430 px, with explicit synthetic viewport/IME checks. No physical Android/iPhone or true mobile-network paint was measured.

### Follow-up: evidence falsification and UX layout

`progression-mobile-offscreen-review-02` repeats only the offscreen case against the same frozen owner snapshot. It confirms the product still reports 19.2 ms for text 8,991.75 px below the viewport. Waiting for the **actual arrival** of explicit smooth scroll (rather than an arbitrary 500 ms) confirms that the return-to-latest button works and that the independent recorder then detects the text. The earlier short-wait assertion was a test assumption, not a product regression.

`progression-mobile-offscreen-control-03` runs the identical case against the ignored reviewer prototype. All three checks pass: offscreen text stays null in both independent and product timing; after explicitly returning, it becomes visible. This establishes known-bad FAIL / known-good PASS for this incident only. The prototype's unrelated dock-height defect remains unaccepted.

The 390 px owner snapshot also shows a layout trade-off, not merely a numerical scroll defect: a 247.5 px panel listing goal and investigation facts plus a separate saved toast pushes almost all completed prose above the screen. The last paragraph ends at y=123 px while the dock starts at y=585 px. The compact prototype leaves the last paragraph at y=375–466 px. A reward summary should not turn every factual observation into a large reward HUD; retain details in the status/log surface and prioritize the actual committed skill, asset or identity changes. This qualitative screenshot review does not claim a human preference experiment.

## Independent fixes accepted for integration, not yet deployed

`artifacts/ui/progression-mobile-visible-frame-fix-01/` replays the same incidents against a frozen snapshot of the reviewer's narrow changes in **current** `public/app.js`. Stop-button focus now survives repeated timer updates; the DOM button is created once. A completed turn 8,991.75 px below the viewport no longer gets a false first-visible measurement; the marker is populated only after the player actually returns to visible text. Completion is stored separately from visibility, and the timing object remains live for a later explicit return. Both old-history identity checks and the independent measurement-order check pass, with no browser error.

This is a **targeted** two-case result, not a full-suite PASS: the same snapshot still has preview/final section difference -18.578 px and generation dock +70/-70 px. Those residual checks remain failed. Frozen candidate-A/B/C real-play results cannot inherit these newer visibility fixes retroactively. The coordinator has separately added `metrics.changeKinds` filtering so investigation facts need not become a large reward card, and the public status drawer now includes real leverage effects/scopes and opportunities. That resolves the earlier missing-public-ledger finding at source level; final browser acceptance still belongs to integration.

## Real browser runs: failures retained rather than selecting a lucky opening

`progression-browser-masked-18-v080a` stopped at catalogue preflight: the frozen A server did not include that world. No model call or game was created, so this is a setup exclusion, not a failed gameplay turn.

`progression-browser-beast-18-v080a` entered the real 390 px mobile browser and submitted the actual first action. Game `game_9f53f4ce5ec74fd58f5cb14923dd6eb1`, trace `trace_6594b8feee3f4f1ab0faae6e0b07369e`. Narrator took 20.835 s, then failed the new literal-evidence gate. The 19.150 s repair failed the same gate; the application ended after 39.988 s with **zero committed turns**. No 15-turn quality conclusion is possible. All metrics and the final game are retained; there was no unrecorded retry.

The exact failure is diagnosed from **retained application `candidateOutputs`**, not guessed from an error message. The narrator wrapped all three citations in extra curly quotation marks. The repair removed the wrappers, but changed the final comma of the departure clause into a full stop, so one `includes()` check still rejected it. This was a formatting failure, not a detected unauthorized ability or ownership change. An initial attempt to use the provider's bounded historical ring encountered truncation; that did **not** mean the application had lost its final outputs. The full application trace at its tail contained both, and `eval/inspect-progression-failure.mjs` now prefers that source.

`eval/progression-evidence-replay.mjs` tests a minimal boundary-quote/trailing-punctuation normalization against both exact failed outputs. Control fails; corrected copies pass the unchanged frozen reducer; six changed-negation/number/ownership/discontinuous-citation negatives remain rejected. `citation-format-replay.json` preserves this as an **offline alternative, not a shipped fix or measured live speedup**. The coordinator chose the simpler design: remove mandatory literal citation matching entirely, retaining full Canon prose plus structural checks. Citations are not semantic truth proofs. The alternative normalizer is therefore not needed in production. No extra model or rule stack was added.

The shared star candidate-C trajectory then exposed a different infrastructure defect at its ninth turn: harmless `available_commands_update` metadata was rejected by an overly broad forbidden-command regex. Both failed traces remain in that sample; this is not evidence that the player used a tool. The requested fix is to distinguish advertised metadata from actual prohibited tool/permission operations, not to give the Narrator tool access. The reviewer's own candidate-C beast sample was authorized for the freed 4318 slot after the masked-C runner exited; it targets **18 turns** and will stop only for a real recorded blocker.

### Candidate-C reader observations, not a 15-turn acceptance

The reviewer has read every committed turn of the shared star-C (8 turns) and masked-C (5 turns) trajectories, along with before/after inventory and money. There are early positive signals **and concrete counterexamples**, not grounds to claim the candidate won:

- Star-C turn5 obtains and actually embeds the tide core; turn6 learns a stable water-diversion technique; turn7 uses it to cross a flooded gap. That is genuine action-space expansion, unlike merely incrementing progress. The same turn records a boat return right with explicit until-second-bell scope; this is useful but **time-limited**, not a permanent enterprise or an enduring identity change. Its use count remains zero at the failed checkpoint.
- Masked-C turns2–3 produce and sell a usable batch; turn5 trades eight coupons plus calibration work for a route map that can support self-directed travel/delivery. The map is a real owned asset, but the sample stopped before repeated use or non-job progression could be observed.
- Masked-C turn3's narration says the ceramic box/product remains with the player and offers to trade it again, while the accepted delta removes the whole ink inventory item and the fact ledger says it stayed with the buyer. Turn4 corrects that ownership in prose, **but invents a cash shortage**: it says the player has eight coupons and cannot afford a20-coupon map. The actual before/after cash is28. Turn5 then deducts8, leaving20. This is not an unaffordable purchase silently accepted by the reducer; it is prose misrepresenting the authoritative balance, influencing the player's route. Source: `progression-masked-18-v080c/turns.jsonl`, indices3–5. Canon/state consistency still needs validation; a progression ledger alone does not solve it.

These short interrupted candidates are not a substitute for the user's >=15-turn sample requirement. Do not promote a production-quality or retention claim from these observations.

### Candidate-C independent run hit preparation, not model prose

`progression-browser-beast-18-v080c` stopped on the first actual browser turn with `PROVIDER_TIMEOUT` after120.009s in `acp_narrator_initialize_auth_session_model_setup`. Trace `trace_bf7da290750245eaaf1edb174ffac17a`, game `game_43a8c169cddc4849a8f6991859873df3`. Headers reached the browser at14.4ms and waiting feedback at24.1ms. There were **zero text events**, no `acp_config_applied`, and no `acp_run_started`; no narrative/choices frame is claimed. The runner exited, preserving0committed/1failed, and its slot was released. These are real failed-request timings, not synthetic replay.

An inspect/list read found a ready empty session in the same isolated workspace at11:48:54.907Z, after the HTTP timeout. This suggests late session creation, but the precise RPC correlation is unavailable, so it is not asserted as a proven root cause. Do not blame120seconds on Narrator output, increase model effort, silently retry, or close an ambiguously owned ACP session.

### Latest full browser integration replay

`progression-mobile-integration-review-02` runs **all32checks** against a fresh snapshot of current shared public source. Thirty pass and two fail; the result remains `passed:false`. Passed: user-up displacement0px, history node/paragraph anchor retention, no pull on long-choice arrival, working explicit return, honest offscreen visibility, stable Stop focus,12px minimum,16px input,44px controls at360/390/430, Arabic direction and draft, IME,75px landscape dock, synthetic keyboard positioning and reduced motion. No page errors occurred. Remaining failures are unchanged: preview/final geometry-18.578px and generation dock+70/-70px. The current reward card is smaller than the earlier six-entry snapshot, but this fixture lacks `changeKinds`, so it does not validate material-only filtering on the actual integrated server. The metadata-to-feedback integration still needs a real response or explicit fixture variant.

## Real browser ACP driver (owned; stopped at documented blocker)

`eval/progression-browser-player.mjs` + `eval/browser-timing-recorder.mjs` are being validated before a bounded 15–40-turn real run on an isolated 4318/4319 service. The driver creates its own game, then enters every action through the actual textarea and submit button. A neutral ACP player observes only the rendered public status panel plus the last four committed narrative turns; it cannot see a Story Brain plan. There is no scripted progression route after opening.

The recorder taps the response reader actually consumed by the UI, correlates `requestId` with backend trace, and separately measures response headers, first read byte, first narrative SSE, immediate feedback visible frame, first narrative visible frame, complete, and next choices visible frame. Visible-frame observation requires two consecutive frames with viewport intersection, accounting for header/dock/open sheets. This remains an explicit **paint proxy**, not a hardware display timestamp. Player deliberation, status inspection and test-driver delays are not counted as application waiting. Failures are retained; version ambiguity stops rather than duplicates an action.

Coordinate the world/server and available model slot with the original coordinator before launching; no third concurrent model prompt. The exact API-player and browser-player observations differ, so comparisons are trajectory evidence, not a strict paired causal A/B.
