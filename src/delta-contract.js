// Resource ceilings are deliberately more tolerant than narrative targets.
// Six valid new facts must not trigger a second model call or discard canon.
export const DELTA_LIMITS = Object.freeze({ factsPerTurn: 20, factChars: 320, inventoryOps: 5, capabilityOps: 3, relationshipChanges: 5, promisesPerTurn: 3, progressPerTurn: 20, coinsPerTurn: 500 });
export function deltaContractText(world) {
  const realmPath = world?.powerSystem?.realms?.map((realm) => realm.name).join(" → ") || "只能进入本世界定义的下一境界";
  return `状态提案契约（初稿与修复共用）：choices 恰好3个{id,label}；delta 表示本轮结束后的净状态，不是所有经手过程，没有变动的字段省略。新采得又当场卖掉的货物不留在inventory；已有物品售出须remove，不能一面写已卖、一面仍add或update为在手。玩家明确只询价、问条件或说先了解再决定时，本轮只得到答复，不提前扣款、转移所有权或承诺交易；已授权的完整行动内可正常结算已知的惯常费用与交易，不拆成逐项确认。交易前核对已有凭证中的已付款、订金和抵扣条件，只扣尚欠余额；取货后更新或移除已结清凭证，不把已付部分再次全额扣掉，也不把未交付的预订当作已获得货物。factsAdd 是字符串数组，优先记录1—5条真正新发生的关键事实，硬上限${DELTA_LIMITS.factsPerTurn}条，每条最多${DELTA_LIMITS.factChars}字符；不要把NPC猜测写成已证实真相。inventoryOps 至多${DELTA_LIMITS.inventoryOps}条，op=add/remove/update；add/remove的qty为整数1—10，remove只可扣现有整件物品。部分使用一包药或一瓶水时用update，给已有id和description写清剩余量，不填qty则保留包/瓶的数量。用掉多件中的几件时必须同步数量：remove.qty表示扣掉几件，或update.qty表示剩余总件数（整数1—10），同时更新description；例如两束用一束，update.qty=1，不能只改说明仍留qty=2。用尽才remove，update不得新增物品或增加件数；capabilityOps 至多${DELTA_LIMITS.capabilityOps}条，op=add/improve，必须给稳定id、名称和可实际使用的description，improve只可更新已有能力。relationshipChanges至多${DELTA_LIMITS.relationshipChanges}条；已有NPC可用现有id和attitude更新，名称沿用Canon；新NPC必须给name。attitude只能是敌视/戒备/陌生/中立/好奇/友善/信任/亲近。promisesAdd/Resolve各至多${DELTA_LIMITS.promisesPerTurn}条，resolve须与已有承诺逐字相同。coinsDelta整数-${DELTA_LIMITS.coinsPerTurn}至${DELTA_LIMITS.coinsPerTurn}，余额不得负数。realmProgressDelta整数0—${DELTA_LIMITS.progressPerTurn}，只有真实修炼或已写清的能力掌握才可增加；本世界境界为 ${realmPath}，realmAdvance必须逐字使用这里列出的下一境界快照名，切换语言不能翻译该值。progress只表示准备与积累，不是身份的第二把尺：当前及本轮Canon已稳定满足下一境界的核心能力、身体/控制与必要资源条件时，同轮写明阶段变化并提交realmAdvance，无须等progress=100。天赋短暂借力、一次侥幸越级或仅获得社会称号不等于全阶段掌握；缺核心条件不能晋升。不能按回合数晋升，也不能因进度满了凭空获得能力。阶段改变时，用capabilityOps.improve同步已经过时的低阶能力描述；不能新条目说能稳定战斗、旧条目仍说现阶段不能战斗。只更新已被Canon突破的限制，天赋固有代价与尚未突破的边界仍保留。realm的unlock描述世界中的下一档机会或资格门槛，不代替已发生的权利授予；组织准入、付费课程和持证资源权仍以实际事实与已有凭证为准。changes可省略，最多8条，每条320字符，最终界面变化由程序根据已应用delta计算。choice.label最多160字符，location/goal最多320字符，物品说明最多360字符，能力说明最多480字符。错误须修改具体字段，不能只把整段正文改写一遍后重复错误。`;
}
